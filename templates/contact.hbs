
 <h1>Contact</h1>
	<div class="row">
		<div class="col-md-6">
		<ul>
		<li>{{#link-to 'phone'}}Phone{{/link-to}}</li>
		<li>{{#link-to 'email'}}Email{{/link-to}}</li>
		</ul>
  
    {{#if messageSent}} 
    <p> The confirmation no. is {{confirmationNo}} and status is {{messageSent}}</p>
    {{else}}
    <p> or,<a href="#" {{action 'showSecret'}}>click here</a> to send</p>
    {{/if}}

</div>
		<div class="col-md-6">
		{{outlet}}
		</div>
	</div>

